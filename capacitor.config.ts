import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-list-sandbox',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
